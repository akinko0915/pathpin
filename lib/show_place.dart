import 'package:flutter/material.dart';

class ShowPlace extends StatelessWidget {
  const ShowPlace({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("show place");
  }
}