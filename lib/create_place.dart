import 'package:flutter/material.dart';

class CreatePlace extends StatelessWidget {
  const CreatePlace({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("create place");
  }
}